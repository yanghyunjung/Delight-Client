import React from "react";
import Footer from "../components/Footer";

const Main = (props) => {
  return (
    <React.Fragment>
      <Footer></Footer>
    </React.Fragment>
  );
};

export default Main;
